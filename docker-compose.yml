version: '3.8'

services:
  photogallery:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: photogallery-app
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - NODE_ENV=production
    restart: unless-stopped
